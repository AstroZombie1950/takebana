# Takebana

Платформа прямых эфиров: производители, заведения, блогеры и эксперты выходят в лайв,
зрители смотрят и общаются. Сервер на Node.js, база MongoDB, ретранслятор Daily.co,
собственный RTMP→HLS конвейер для массовых трансляций.

Репозиторий заведён с нуля 4 сентября 2026. Истории от прежней команды нет —
исходники получены архивом, оригинал лежит в `temp/views.zip` вне репозитория.

## Что где

| Папка | Что это |
|---|---|
| `server/` | Приложение: Express + EJS + Socket.IO + MongoDB. Код клиента, приводим в порядок |
| `landing/` | Маркетинговый лендинг (статика, без сборки). Позже станет главной страницей продукта |
| `ops/` | Эксплуатация: pm2, nginx, деплой |
| `docs/` | Документация проекта: карта кода, аудит, роадмап, шпаргалка по Node |
| `temp/` | Черновики, архивы, документы для клиента, секреты. **В репозиторий не идёт** |

## Быстрый старт

Нужны Node 20 LTS (на проде именно 20) и MongoDB.

```bash
cd server
cp .env.example .env      # заполнить ключи
npm install
npm run dev               # nodemon, перезапуск при правке файлов
```

Открыть http://localhost:3000

Лендинг открывается без сервера — просто откройте `landing/index.html`,
либо `npx serve landing` для честного http.

Карта кода — [docs/STRUCTURE.md](docs/STRUCTURE.md). Как работать с Node — [docs/NODE.md](docs/NODE.md).

## Состояние

**Где мы остановились — [docs/STATUS.md](docs/STATUS.md).**

Что работает, что нет и в каком порядке чиним — [docs/AUDIT.md](docs/AUDIT.md).
План работ и границы ответственности — [docs/ROADMAP.md](docs/ROADMAP.md).
Чек-лист переезда — [docs/MIGRATION.md](docs/MIGRATION.md).
